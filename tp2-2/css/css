 html {
    overflow-x: hidden;
}
{
    box-sizing: border-box;
}
user agent stylesheet
head {
    display: none;
}
body {
    font-size: 16px;
    font-family: Arial,Helvetica,sans-serif;
    line-height: 1.6;
}
